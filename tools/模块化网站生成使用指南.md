# Docms 模块化网站生成使用指南

**版本**: 1.0
**更新日期**: 2025-11-02

---

## 📚 目录

- [1. 概述](#1-概述)
- [2. 快速开始](#2-快速开始)
- [3. 使用方式](#3-使用方式)
- [4. 预设方案](#4-预设方案)
- [5. 自定义模块选择](#5-自定义模块选择)
- [6. 模块依赖关系](#6-模块依赖关系)
- [7. 完整示例](#7-完整示例)
- [8. 常见问题](#8-常见问题)

---

## 1. 概述

### 1.1 为什么需要模块化生成？

传统的 scaffold 会生成包含所有模块的完整项目，这导致：
- ❌ 项目包含大量不需要的代码
- ❌ 数据库包含不必要的表
- ❌ 增加维护复杂度
- ❌ 浪费存储空间

模块化生成的优势：
- ✅ 只生成需要的模块
- ✅ 轻量化的项目结构
- ✅ 更好的维护性
- ✅ 按需扩展

### 1.2 核心文件

模块化系统包含以下核心文件：

```
docms-scaffold/
├── modules_config.yaml          # 模块配置文件（定义所有模块）
├── module_manager.py             # 模块管理器（处理依赖和生成）
├── create_project_modular.py    # 模块化项目生成器
└── create_project.py             # 原始生成器（仍可使用）
```

---

## 2. 快速开始

### 2.1 最简单的方式

使用默认配置（企业官网方案）：

```bash
cd docms-scaffold
python create_project_modular.py my-website
```

这将创建一个包含以下模块的项目：
- 核心模块（必需）
- 新闻/博客
- 团队展示
- 案例展示
- 产品/服务
- 常见问题
- 图片画廊

### 2.2 使用预设方案

```bash
# 电商网站
python create_project_modular.py my-shop --preset ecommerce

# 教育培训网站
python create_project_modular.py my-school --preset education

# 餐厅网站
python create_project_modular.py my-restaurant --preset restaurant
```

### 2.3 查看可用选项

```bash
# 查看所有预设方案
python create_project_modular.py --list-presets

# 查看所有可用模块
python create_project_modular.py --list-modules

# 查看帮助
python create_project_modular.py --help
```

---

## 3. 使用方式

### 3.1 命令行参数

```bash
python create_project_modular.py <project_name> [options]
```

**参数说明**:

| 参数 | 简写 | 说明 | 示例 |
|------|------|------|------|
| `project_name` | - | 项目名称（必需） | `my-website` |
| `project_path` | - | 项目路径（可选） | `/path/to/project` |
| `--preset` | `-p` | 使用预设方案 | `--preset ecommerce` |
| `--modules` | `-m` | 自定义模块列表 | `--modules team,portfolio,product` |
| `--interactive` | `-i` | 交互式选择 | `--interactive` |
| `--list-presets` | - | 列出所有预设方案 | - |
| `--list-modules` | - | 列出所有可用模块 | - |

### 3.2 四种使用模式

#### 模式1: 默认模式（推荐用于企业官网）

```bash
python create_project_modular.py my-website
```

自动使用 `corporate` 预设方案。

#### 模式2: 预设方案模式（推荐用于特定行业）

```bash
python create_project_modular.py my-website --preset <preset_name>
```

可用预设方案：
- `corporate` - 企业官网
- `ecommerce` - 电商网站
- `education` - 教育培训
- `restaurant` - 餐厅/咖啡馆
- `medical` - 医疗/诊所
- `service` - 专业服务
- `minimal` - 最小化配置
- `full` - 完整配置

#### 模式3: 自定义模块模式（适合高级用户）

```bash
python create_project_modular.py my-website --modules team,portfolio,product,faq
```

手动指定需要的模块，系统会自动解析依赖关系。

#### 模式4: 交互式模式（适合不确定需求的用户）

```bash
python create_project_modular.py my-website --interactive
```

通过交互式界面选择预设方案或自定义模块。

---

## 4. 预设方案

### 4.1 企业官网 (corporate)

**适用场景**: 标准企业官网、公司简介站点

**包含模块**:
- ✅ 新闻/博客 (post)
- ✅ 团队展示 (team)
- ✅ 案例展示 (portfolio)
- ✅ 产品/服务 (product)
- ✅ 常见问题 (faq)
- ✅ 图片画廊 (gallery)

**使用示例**:
```bash
python create_project_modular.py acme-corp --preset corporate
```

---

### 4.2 电商网站 (ecommerce)

**适用场景**: 在线商城、零售网站

**包含模块**:
- ✅ 产品管理 (product)
- ✅ 自定义字段 (custom_field)
- ✅ 用户/会员 (user)
- ✅ 购物车 (cart)
- ✅ 订单管理 (order)
- ✅ 评论/评价 (comment)
- ✅ 通讯订阅 (newsletter)
- ✅ 图片画廊 (gallery)

**使用示例**:
```bash
python create_project_modular.py my-shop --preset ecommerce
```

---

### 4.3 教育培训 (education)

**适用场景**: 学校、培训机构、在线教育平台

**包含模块**:
- ✅ 新闻/博客 (post)
- ✅ 团队展示 (team) - 师资团队
- ✅ 产品/服务 (product) - 课程
- ✅ 常见问题 (faq)
- ✅ 用户/会员 (user)
- ✅ 在线预约 (booking) - 试听预约
- ✅ 活动报名 (event)
- ✅ 视频展示 (video) - 教学视频
- ✅ 文件下载 (file_download) - 课程资料

**使用示例**:
```bash
python create_project_modular.py language-school --preset education
```

---

### 4.4 餐厅/咖啡馆 (restaurant)

**适用场景**: 餐厅、咖啡馆、快餐店

**包含模块**:
- ✅ 餐厅订餐 (restaurant)
- ✅ 在线预约 (booking) - 订座
- ✅ 图片画廊 (gallery) - 环境、菜品
- ✅ 评论/评价 (comment)
- ✅ 通讯订阅 (newsletter)

**使用示例**:
```bash
python create_project_modular.py my-cafe --preset restaurant
```

---

### 4.5 医疗/诊所 (medical)

**适用场景**: 诊所、牙医、理疗中心

**包含模块**:
- ✅ 新闻/博客 (post) - 健康资讯
- ✅ 团队展示 (team) - 医生团队
- ✅ 产品/服务 (product) - 医疗服务
- ✅ 常见问题 (faq)
- ✅ 在线预约 (booking)
- ✅ 视频展示 (video) - 健康讲座
- ✅ 文件下载 (file_download) - 健康手册

**使用示例**:
```bash
python create_project_modular.py dental-clinic --preset medical
```

---

### 4.6 专业服务 (service)

**适用场景**: 律师、会计、咨询公司

**包含模块**:
- ✅ 新闻/博客 (post) - 专业文章
- ✅ 团队展示 (team)
- ✅ 案例展示 (portfolio)
- ✅ 产品/服务 (product)
- ✅ 常见问题 (faq)
- ✅ 在线预约 (booking)
- ✅ 文件下载 (file_download) - 资料、模板

**使用示例**:
```bash
python create_project_modular.py law-firm --preset service
```

---

### 4.7 最小化配置 (minimal)

**适用场景**: 仅需要最基础功能的网站

**包含模块**:
- ✅ 仅核心模块（媒体文件、站点设置、联系表单）

**使用示例**:
```bash
python create_project_modular.py simple-site --preset minimal
```

---

### 4.8 完整配置 (full)

**适用场景**: 需要所有功能的复杂网站

**包含模块**:
- ✅ 所有 20 个模块

**使用示例**:
```bash
python create_project_modular.py full-featured-site --preset full
```

---

## 5. 自定义模块选择

### 5.1 可用模块列表

#### 核心模块（自动包含，不可禁用）
- `base` - 基础模块
- `media` - 媒体文件
- `site` - 站点设置
- `contact` - 联系表单

#### 基础内容模块
- `post` - 新闻/博客
- `team` - 团队展示
- `portfolio` - 案例展示
- `product` - 产品/服务
- `custom_field` - 自定义字段（产品规格）
- `faq` - 常见问题

#### 交互功能模块
- `comment` - 评论/评价
- `user` - 用户/会员
- `newsletter` - 通讯订阅

#### 电商交易模块
- `cart` - 购物车
- `order` - 订单管理

#### 预约与服务模块
- `booking` - 在线预约
- `restaurant` - 餐厅订餐
- `event` - 活动报名

#### 多媒体与资源模块
- `gallery` - 图片画廊
- `video` - 视频展示
- `file_download` - 文件下载

### 5.2 自定义模块示例

#### 示例1: 创建简单的博客网站

```bash
python create_project_modular.py my-blog --modules post,comment,user,newsletter
```

包含功能：
- ✓ 文章发布
- ✓ 用户评论
- ✓ 用户注册登录
- ✓ 邮件订阅

#### 示例2: 创建作品集网站

```bash
python create_project_modular.py my-portfolio --modules portfolio,gallery,video
```

包含功能：
- ✓ 项目案例展示
- ✓ 图片画廊
- ✓ 视频展示

#### 示例3: 创建预约服务网站

```bash
python create_project_modular.py booking-site --modules team,booking,user,faq
```

包含功能：
- ✓ 团队成员展示
- ✓ 在线预约
- ✓ 用户系统
- ✓ 常见问题

---

## 6. 模块依赖关系

### 6.1 依赖说明

某些模块依赖其他模块才能正常工作。当你选择一个模块时，系统会自动包含其依赖模块。

### 6.2 依赖关系表

| 模块 | 依赖模块 | 说明 |
|------|---------|------|
| `post` | `media` | 文章需要封面图片 |
| `team` | `media` | 团队成员需要照片 |
| `portfolio` | `media` | 案例需要图片展示 |
| `product` | `media`, `custom_field` | 产品需要图片和自定义字段 |
| `cart` | `product`, `user` | 购物车需要产品和用户 |
| `order` | `product`, `user`, `cart` | 订单需要产品、用户和购物车 |
| `booking` | `team` | 预约需要服务人员 |
| `restaurant` | `media` | 餐厅菜单需要菜品图片 |
| `event` | `media` | 活动需要封面图 |
| `gallery` | `media` | 画廊需要图片 |
| `video` | `media` | 视频需要缩略图 |
| `file_download` | `media` | 文件下载需要文件管理 |

### 6.3 依赖自动解析示例

当你选择 `order` 模块时：

```bash
python create_project_modular.py my-shop --modules order
```

系统会自动包含：
- ✅ order（订单管理）
- ✅ cart（购物车 - order 的依赖）
- ✅ product（产品 - cart 的依赖）
- ✅ user（用户 - cart 的依赖）
- ✅ custom_field（自定义字段 - product 的依赖）
- ✅ media（媒体 - product 的依赖）
- ✅ 所有核心模块

---

## 7. 完整示例

### 7.1 创建电商网站完整流程

```bash
# 1. 进入 scaffold 目录
cd docms-scaffold

# 2. 使用电商预设方案创建项目
python create_project_modular.py nz-shop --preset ecommerce

# 输出示例:
创建项目: nz-shop
目标路径: D:\projects\nz-shop
使用预设方案: ecommerce

启用的模块 (12个):
  ✓ base            - 基础模块
  ✓ cart            - 购物车
  ✓ comment         - 评论/评价
  ✓ contact         - 联系表单
  ✓ custom_field    - 自定义字段
  ✓ gallery         - 图片画廊
  ✓ media           - 媒体文件
  ✓ newsletter      - 通讯订阅
  ✓ order           - 订单管理
  ✓ product         - 产品/服务
  ✓ site            - 站点设置
  ✓ user            - 用户/会员

📦 复制基础文件...
  ✓ alembic.ini
  ✓ app/
  ...

📦 复制模块文件...
  ✓ app/models/product.py
  ✓ app/models/cart.py
  ✓ app/models/order.py
  ...

📝 生成 __init__.py 文件...
  ✓ app/models/__init__.py
  ✓ app/services/__init__.py
  ✓ app/schemas/__init__.py

🔧 处理模板文件...
  ✓ app/config.py

✅ 项目 'nz-shop' 创建完成!
📁 位置: D:\projects\nz-shop

# 3. 进入项目目录
cd nz-shop

# 4. 创建虚拟环境
python -m venv venv

# 5. 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 6. 安装依赖
pip install -r requirements.txt

# 7. 运行数据库迁移
alembic upgrade head

# 8. 启动项目
python app.py
```

### 7.2 创建教育网站自定义模块

```bash
# 使用自定义模块创建
python create_project_modular.py language-school \
  --modules post,team,product,user,booking,event,video,file_download

# 系统会自动添加依赖:
启用的模块 (14个):
  ✓ base            - 基础模块
  ✓ booking         - 在线预约
  ✓ contact         - 联系表单
  ✓ event           - 活动报名
  ✓ file_download   - 文件下载
  ✓ media           - 媒体文件
  ✓ post            - 新闻/博客
  ✓ product         - 产品/服务
  ✓ site            - 站点设置
  ✓ team            - 团队展示
  ✓ user            - 用户/会员
  ✓ video           - 视频展示
  ✓ custom_field    - 自定义字段（product 的依赖）
  ✓ ... (其他依赖)
```

### 7.3 交互式创建流程

```bash
python create_project_modular.py my-website --interactive

# 输出:
=== 模块选择 ===

可用预设方案:
  1. 企业官网       - 标准企业官网配置
  2. 电商网站       - 完整电商解决方案
  3. 教育培训       - 教育机构网站
  4. 餐厅/咖啡馆    - 餐饮行业网站
  5. 医疗/诊所      - 医疗健康服务
  6. 专业服务       - 律师、会计、咨询等专业服务
  7. 最小化配置     - 仅包含核心功能
  8. 完整配置       - 包含所有模块
  9. 自定义选择

请选择 (1-9): 3

已选择: 教育培训

启用的模块 (12个):
...
```

---

## 8. 常见问题

### 8.1 如何查看项目使用了哪些模块？

项目生成后会在根目录创建 `enabled_modules.txt` 文件，记录了所有启用的模块。

```bash
cat enabled_modules.txt

# 输出示例:
# Enabled Modules for this project
# Generated by create_project_modular.py

base
cart
comment
contact
custom_field
gallery
media
newsletter
order
product
site
user
```

### 8.2 生成后可以添加新模块吗？

可以，但需要手动操作：
1. 从 scaffold 复制对应的 model/service/schema 文件
2. 更新 `app/models/__init__.py` 等导入文件
3. 创建 Alembic 迁移脚本
4. 运行 `alembic upgrade head`

建议：**在项目初期就确定好需要的模块**。

### 8.3 如何选择合适的预设方案？

根据业务类型选择：

| 业务类型 | 推荐方案 |
|---------|---------|
| 公司官网 | corporate |
| 在线商城 | ecommerce |
| 学校/培训 | education |
| 餐厅/咖啡馆 | restaurant |
| 诊所/牙医 | medical |
| 律师/会计/咨询 | service |
| 不确定 | 使用 `--interactive` 交互式选择 |

### 8.4 依赖的模块能禁用吗？

不能。依赖模块会自动包含，因为上层模块需要它们才能正常工作。

例如：选择 `order` 会自动包含 `cart`, `product`, `user`等。

### 8.5 如何创建完全自定义的配置？

1. 方式1: 使用 `--modules` 参数
```bash
python create_project_modular.py my-site --modules module1,module2,module3
```

2. 方式2: 修改 `modules_config.yaml` 添加自己的预设方案

3. 方式3: 使用 `--interactive` 交互式选择

### 8.6 核心模块包含哪些内容？

核心模块（自动包含，不可禁用）：
- `base` - BaseModel 基础模型类
- `media` - MediaFile 媒体文件管理
- `site` - SiteColumn, SinglePage, SiteSetting 站点配置
- `contact` - ContactMessage 联系表单

### 8.7 生成的项目结构是什么样的？

```
my-website/
├── alembic.ini
├── app/
│   ├── models/           # 只包含选择的模块
│   │   ├── __init__.py   # 自动生成，只导入选择的模块
│   │   ├── base.py       # 核心模块
│   │   ├── media.py      # 核心模块
│   │   └── ...           # 其他选择的模块
│   ├── services/         # 只包含选择的模块
│   │   ├── __init__.py
│   │   └── ...
│   ├── schemas/          # 只包含选择的模块
│   │   ├── __init__.py
│   │   └── ...
│   └── ...
├── enabled_modules.txt   # 记录启用的模块
├── requirements.txt
└── ...
```

### 8.8 如何添加新的预设方案？

编辑 `modules_config.yaml` 文件，在 `presets` 部分添加：

```yaml
presets:
  - name: "my_preset"
    display_name: "我的预设"
    description: "自定义预设方案"
    modules:
      - "post"
      - "team"
      - "gallery"
```

然后即可使用：
```bash
python create_project_modular.py my-site --preset my_preset
```

---

## 9. 总结

### 9.1 推荐使用流程

1. **确定需求** → 明确网站需要哪些功能
2. **选择方案** → 使用预设方案或自定义模块
3. **生成项目** → 运行 `create_project_modular.py`
4. **初始化** → 安装依赖、运行迁移
5. **开发** → 开始开发业务逻辑

### 9.2 最佳实践

✅ **DO**（推荐做法）:
- 使用预设方案快速开始
- 在项目初期确定好所有需要的模块
- 记录项目使用的模块（通过 `enabled_modules.txt`）
- 使用 `--list-modules` 了解可用模块

❌ **DON'T**（不推荐做法）:
- 不要生成后再频繁添加/删除模块
- 不要禁用依赖模块
- 不要使用 `full` 方案除非真的需要所有功能

### 9.3 获取帮助

```bash
# 查看帮助
python create_project_modular.py --help

# 查看所有预设
python create_project_modular.py --list-presets

# 查看所有模块
python create_project_modular.py --list-modules
```

---

**文档结束**

如有问题请参考 `Docms网站系统20个核心模块说明文档.md` 了解各模块详细功能。
